package dao.worker;

/**
 * This enum contains all column names in order. The first name
 * is the name of the table itself.
 */
public enum WorkerColumns
{
	Workers,
	id,
	firstname,
	surname,
	email,
	birthday,
	pass
}
